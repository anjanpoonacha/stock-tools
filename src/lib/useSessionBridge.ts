// src/lib/useSessionBridge.ts
// Legacy wrapper for backward compatibility
// Now uses unified SessionContext instead of making duplicate API calls
export { useSessionBridge } from '@/contexts/SessionContext';
